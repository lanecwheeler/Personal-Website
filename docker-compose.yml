services:
  web:
    build: .
    ports:
      - "8080:80"
    networks:
      - app-network

  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
