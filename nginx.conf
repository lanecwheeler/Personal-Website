server {
    listen 80;
    listen [::]:80;
    server_name lanewheeler.dev www.lanewheeler.dev;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://api:3000;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
